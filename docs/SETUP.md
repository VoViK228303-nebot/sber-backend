# Руководство по установке и запуску

Подробная инструкция по развёртыванию банковского приложения SberBank Online.

## 📋 Содержание

- [Системные требования](#системные-требования)
- [Быстрый старт](#быстрый-старт)
- [Пошаговая установка](#пошаговая-установка)
- [Конфигурация](#конфигурация)
- [Запуск проекта](#запуск-проекта)
- [Управление базой данных](#управление-базой-данных)
- [Тестовые данные](#тестовые-данные)
- [Устранение неполадок](#устранение-неполадок)

## 🖥️ Системные требования

### Обязательные компоненты

| Компонент | Минимальная версия | Рекомендуемая |
|-----------|-------------------|---------------|
| Node.js | 18.x | 20.x LTS |
| npm | 9.x | 10.x |
| Docker | 20.10.x | 24.x |
| Docker Compose | 2.x | 2.20.x |
| Git | 2.30.x | 2.40.x |

### Системные ресурсы

- **RAM**: минимум 4 GB (рекомендуется 8 GB)
- **Диск**: минимум 2 GB свободного места
- **CPU**: 2+ ядра

### Поддерживаемые ОС

- ✅ Windows 10/11 (WSL2 рекомендуется)
- ✅ macOS 12+ (Intel и Apple Silicon)
- ✅ Linux (Ubuntu 20.04+, Debian 11+, CentOS 8+)

## 🚀 Быстрый старт

Если у вас уже установлены все зависимости:

```bash
# 1. Клонирование репозитория
git clone <repository-url>
cd sberbank-online

# 2. Запуск автоматической установки
npm run setup

# 3. Запуск проекта
npm run dev
```

Приложение будет доступно:
- 🌐 Фронтенд: http://localhost:5173
- 🔌 API: http://localhost:3001
- 🗄️ Adminer (БД): http://localhost:8080

## 📦 Пошаговая установка

### Шаг 1: Установка Node.js

#### Windows

1. Скачайте установщик с [nodejs.org](https://nodejs.org/)
2. Запустите установщик и следуйте инструкциям
3. Проверьте установку:

```bash
node --version  # v20.x.x
npm --version   # 10.x.x
```

#### macOS

```bash
# Используя Homebrew
brew install node

# Проверка
node --version
npm --version
```

#### Linux (Ubuntu/Debian)

```bash
# Установка через NodeSource
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверка
node --version
npm --version
```

### Шаг 2: Установка Docker

#### Windows

1. Установите [Docker Desktop](https://www.docker.com/products/docker-desktop)
2. Убедитесь, что включён WSL2
3. Перезагрузите компьютер

#### macOS

```bash
brew install --cask docker
```

#### Linux

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install docker.io docker-compose-plugin

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

### Шаг 3: Клонирование репозитория

```bash
git clone <repository-url>
cd sberbank-online
```

### Шаг 4: Установка зависимостей

```bash
# Установка всех зависимостей (корень + все приложения)
npm install

# Или установка отдельных частей
npm install                    # корневые зависимости
npm install --workspace=web    # фронтенд
npm install --workspace=api    # бэкенд
```

### Шаг 5: Настройка переменных окружения

```bash
# Создание .env файлов из шаблонов
copy apps\api\.env.example apps\api\.env      # Windows
cp apps/api/.env.example apps/api/.env         # Linux/Mac

copy apps\web\.env.example apps\web\.env      # Windows
cp apps/web/.env.example apps/web/.env         # Linux/Mac
```

Отредактируйте файлы `.env` при необходимости (обычно значения по умолчанию подходят для локальной разработки).

### Шаг 6: Запуск базы данных

```bash
# Запуск PostgreSQL и Redis в Docker
docker-compose up -d

# Проверка статуса контейнеров
docker-compose ps
```

### Шаг 7: Инициализация базы данных

```bash
# Генерация клиента Prisma
npm run db:generate

# Выполнение миграций
npm run db:migrate

# Заполнение тестовыми данными
npm run db:seed
```

## ⚙️ Конфигурация

### Переменные окружения API

Файл: `apps/api/.env`

```env
# Сервер
NODE_ENV=development
PORT=3001
API_URL=http://localhost:3001

# База данных
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sberbank?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# Логирование
LOG_LEVEL=debug
```

### Переменные окружения фронтенда

Файл: `apps/web/.env`

```env
# API
VITE_API_URL=http://localhost:3001/api

# Приложение
VITE_APP_NAME=SberBank Online
VITE_APP_VERSION=1.0.0
```

### Docker Compose конфигурация

Файл: `docker-compose.yml`

```yaml
services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sberbank
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  adminer:
    image: adminer:latest
    ports:
      - "8080:8080"
```

## ▶️ Запуск проекта

### Режим разработки

```bash
# Запуск всех сервисов одной командой
npm run dev

# Или отдельно:
npm run dev:api    # только бэкенд
npm run dev:web    # только фронтенд
```

### Продакшен режим

```bash
# Сборка
npm run build

# Запуск
npm start
```

### Проверка работоспособности

1. Откройте http://localhost:5173 - должен загрузиться фронтенд
2. Откройте http://localhost:3001/api/health - должен вернуться статус "ok"
3. Откройте http://localhost:8080 - должен открыться Adminer

## 🗄️ Управление базой данных

### Полезные команды

```bash
# Prisma Studio (визуальный редактор БД)
npm run db:studio

# Создание новой миграции
npm run db:migrate:dev -- --name migration_name

# Сброс базы данных
npm run db:reset

# Просмотр статуса миграций
npm run db:status

# Валидация схемы
npm run db:validate
```

### Резервное копирование

```bash
# Экспорт данных
docker exec -t sberbank-online-postgres-1 pg_dump -U postgres sberbank > backup.sql

# Импорт данных
docker exec -i sberbank-online-postgres-1 psql -U postgres sberbank < backup.sql
```

## 🧪 Тестовые данные

После выполнения `npm run db:seed` будут созданы следующие данные:

### Пользователь 1

- **Email**: `ivan@example.com`
- **Пароль**: `password123`
- **Имя**: Иван Петров
- **Счета**: Дебетовый, Кредитный, Накопительный
- **Баланс**: ~150,000 ₽ суммарно

### Пользователь 2

- **Email**: `maria@example.com`
- **Пароль**: `password123`
- **Имя**: Мария Сидорова
- **Счета**: Дебетовый, Накопительный
- **Баланс**: ~250,000 ₽ суммарно

### Операции

- История переводов между счетами
- Платежи и пополнения
- Различные статусы операций

## 🔧 Устранение неполадок

### Порт уже занят

**Ошибка**: `Error: listen EADDRINUSE: address already in use :::3001`

**Решение**:
```bash
# Windows
netstat -ano | findstr :3001
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:3001 | xargs kill -9
```

### Ошибка подключения к базе данных

**Ошибка**: `Can't reach database server at localhost:5432`

**Решение**:
1. Проверьте, запущен ли Docker:
   ```bash
   docker-compose ps
   ```
2. Перезапустите контейнеры:
   ```bash
   docker-compose down
   docker-compose up -d
   ```
3. Проверьте логи:
   ```bash
   docker-compose logs postgres
   ```

### Ошибка Prisma

**Ошибка**: `PrismaClientInitializationError`

**Решение**:
```bash
# Перегенерация клиента
npm run db:generate

# Проверка схемы
npm run db:validate
```

### CORS ошибки

**Ошибка**: `CORS policy: No 'Access-Control-Allow-Origin' header`

**Решение**:
1. Проверьте `CORS_ORIGIN` в `apps/api/.env`
2. Убедитесь, что фронтенд запущен на указанном порту
3. Перезапустите API сервер

### Ошибка модулей Node.js

**Ошибка**: `Cannot find module '...'`

**Решение**:
```bash
# Очистка кэша
npm cache clean --force

# Переустановка зависимостей
rm -rf node_modules
rm -rf apps/*/node_modules
npm install
```

### Docker не запускается

**Ошибка**: `Cannot connect to the Docker daemon`

**Решение**:
1. Убедитесь, что Docker Desktop запущен
2. Проверьте права доступа (Linux):
   ```bash
   sudo usermod -aG docker $USER
   newgrp docker
   ```

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте [CHANGELOG.md](../CHANGELOG.md) на наличие известных проблем
2. Изучите логи приложения
3. Создайте issue с описанием проблемы

## 📚 Дополнительная документация

- [API Specification](API_SPEC.md) - документация API
- [Architecture](ARCHITECTURE.md) - архитектура проекта
- [Design System](DESIGN_SYSTEM.md) - дизайн-система
