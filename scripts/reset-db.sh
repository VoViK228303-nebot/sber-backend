#!/bin/bash

# ==========================================
# 🏦 SBER Банк - Сброс базы данных
# ==========================================

set -e

# Цвета для вывода
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo -e "${BLUE}==========================================${NC}"
echo -e "${BLUE}🏦 SBER Банк - Сброс базы данных${NC}"
echo -e "${BLUE}==========================================${NC}"
echo ""

# Переход в директорию API
cd "$(dirname "$0")/../apps/api"

echo -e "${YELLOW}⚠️  ВНИМАНИЕ: Это действие удалит ВСЕ данные из базы данных!${NC}"
echo ""
read -p "Вы уверены? (y/N): " -n 1 -r
echo ""

if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo -e "${YELLOW}❌ Операция отменена${NC}"
    exit 0
fi

echo ""
echo -e "${YELLOW}🗑️  Сброс базы данных...${NC}"
npx prisma migrate reset --force --skip-generate

echo ""
echo -e "${YELLOW}🌱 Пересидирование базы данных...${NC}"
npx prisma db seed

echo ""
echo -e "${GREEN}✅ База данных успешно сброшена и заполнена тестовыми данными${NC}"
echo ""
